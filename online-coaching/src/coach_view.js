import React from 'react'
import ReactDOM from 'react-dom'
import {Base} from './index.js'

class Coach extends React.Component {
    render() {
        return(
            <div>
                <Base/>
                //For loop/way to display links to all 32 rowers
            </div>
        )
    }
}